# python

Python projects, each independently deployable as a Docker container and published to GitHub Container Registry.

## Projects

### [solar-cli](projects/solar-cli/)

Interactive CLI that queries solar production data in InfluxDB using natural language. Powered by Claude's tool-use API — ask a question, and it writes and executes Flux queries behind the scenes.
